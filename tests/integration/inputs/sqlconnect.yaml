connections:
  Postgres_Test:
    sqlalchemy_driver: 'postgresql+psycopg2'
    server: 'db:5432'
    odbc_driver: ''
    database: 'postgres'
    username: '${POSTGRES_USERNAME}'
    password: '${POSTGRES_PASSWORD}'
